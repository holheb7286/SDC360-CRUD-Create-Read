# SDC360-CRUD-Create-Read
Booklist web application using the MEAN stack that has the ability to Create and Read records in a MongoDB database.

git commit -m "Completed CRUD Create Read guided practice"
